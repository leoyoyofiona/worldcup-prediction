services:
  - type: web
    name: worldcup-prediction
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/status
    envVars:
      - key: TOURNAMENT_SIMULATIONS
        value: "15000"
